import { SectionPage } from "@/components/section-page";
import { sectionPages } from "@/lib/page-content";

export default function DashboardPage() {
  return <SectionPage page={sectionPages.dashboard} />;
}
